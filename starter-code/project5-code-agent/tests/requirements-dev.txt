# Project 5 测试依赖
#
# 本项目 P0 修复回归测试（tests/test_p0_fixes.py）使用标准库 unittest，
# 无需安装额外依赖即可运行。
#
# 运行方式：
#   cd project5-code-agent
#   python -m unittest tests.test_p0_fixes -v
#
# 现有 tests/test_sandbox.py 需要 E2B API Key，可设置 SKIP_E2B_TESTS=1 跳过：
#   SKIP_E2B_TESTS=1 python -m unittest discover -s tests -v
